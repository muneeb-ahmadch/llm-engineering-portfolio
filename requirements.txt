# Python 3.12. Not 3.14: torch has no wheels for it yet.
#   uv venv --python 3.12 .venv
#   uv pip install --python .venv/bin/python -r requirements.txt

# Retrieval and reranking (experiments 01 to 06). Local, CPU, no API key, no spend.
sentence-transformers>=3.0
numpy<2.3

# Generation, judging, the agent loop (experiments 07 onward).
openai>=1.40
python-dotenv>=1.0

# Tracing (experiment 10 onward).
langfuse>=4.14

# MCP server (experiment 14). Pinned: mcp.server.fastmcp does not exist on 2.x,
# the class is MCPServer. See projects/14-mcp/results.md.
mcp==2.0.0

# Stateful graphs (experiment 15). Pinned to what was measured.
langgraph==1.2.11
langgraph-checkpoint==4.2.0
langgraph-checkpoint-sqlite==3.1.1
langchain-core==1.5.4
pydantic>=2.0
